"""P10 (optional) — V-JEPA-style TemporalConnector pretraining (PLAN.md P10). Stub — P10.

Masked spatiotemporal latent prediction on unlabeled surgical video; EMA teacher;
smooth-L1 + variance/covariance anti-collapse. LLM not involved (~4-6 GB VRAM).
Output temporal_connector_jepa.pt -> sft --connector-init.
"""
from __future__ import annotations


def main(argv=None):
    raise NotImplementedError("P10 optional")
